## Fibonacci Sequence (Recursive)

### تعریف

**Fibonacci Sequence** دنباله‌ای از اعداد است که در آن هر عدد برابر با **جمع دو عدد قبلی** است.

در این پیاده‌سازی:

* جمله اول = 0
* جمله دوم = 1
* از جمله سوم به بعد:

[
F(n) = F(n-1) + F(n-2)
]

### مثال دنباله

```
n : 1  2  3  4  5  6  7
F : 0  1  1  2  3  5  8
```

### ایده کلی

* از **بازگشت (Recursion)** برای محاسبه مقدار فیبوناچی استفاده می‌شود.
* برای `n = 1` و `n = 2` مقدار مشخص و ثابت برگردانده می‌شود.
* برای سایر مقادیر، تابع خودش را دوبار صدا می‌زند.
* این مثال به خوبی **عملکرد پشته فراخوانی (Call Stack)** را نشان می‌دهد.

### نکات آموزشی

* این روش پیاده‌سازی **ساده ولی ناکارآمد** است.
* پیچیدگی زمانی:
  **O(2ⁿ)**
* برای مقادیر بزرگ `n` باعث کندی شدید و حتی **Stack Overflow** می‌شود.
* مناسب برای **آموزش مفهوم بازگشت**، نه استفاده عملی.

---

### 🧪 مثال اجرا

```bash
./06_fibonacci 10
```

خروجی:

```
34
```

### [Back](/readme.md)