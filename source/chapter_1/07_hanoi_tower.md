## Tower of Hanoi (Recursive)

### تعریف

**Tower of Hanoi** یک مسئله کلاسیک در ساختمان داده و الگوریتم‌هاست که برای آموزش **بازگشت (Recursion)** و **پشته فراخوانی** استفاده می‌شود.

مسئله شامل:

* سه میله (میخ):

  * A (مبدأ / Source)
  * B (کمکی / Auxiliary)
  * C (مقصد / Destination)
* تعدادی دیسک با اندازه‌های مختلف که ابتدا همگی روی میله A قرار دارند.

### قوانین مسئله

1. در هر حرکت فقط **یک دیسک** می‌توان جابه‌جا کرد.
2. هیچ‌وقت نباید **دیسک بزرگ‌تر روی دیسک کوچک‌تر** قرار بگیرد.
3. هدف: انتقال همه دیسک‌ها از میله A به میله C.

---

### ایده کلی حل

* اگر فقط **یک دیسک** وجود داشته باشد، آن را مستقیم از A به C منتقل می‌کنیم.
* اگر بیش از یک دیسک وجود داشته باشد:

  1. انتقال `n-1` دیسک از A به B با کمک C
  2. انتقال دیسک آخر از A به C
  3. انتقال `n-1` دیسک از B به C با کمک A

این فرآیند به صورت **بازگشتی** انجام می‌شود.

---

### نکات آموزشی

* نمایش بسیار واضحی از **بازگشت و Call Stack**
* تعداد حرکات مورد نیاز:
  [
  2^n - 1
  ]
* پیچیدگی زمانی:
  **O(2ⁿ)**
* مناسب برای درک:

  * تفکر بازگشتی
  * تقسیم مسئله به زیرمسئله‌های کوچک‌تر

---

### 🧪 مثال اجرا

```bash
./07_hanoi_tower 2
```

خروجی:

```
Move disk 1 from A to B
Move disk 2 from A to C
Move disk 1 from B to C
```