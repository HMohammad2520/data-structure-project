## Linear Search (جستجوی خطی)

### 1️⃣ تعریف

**Linear Search** الگوریتمی ساده برای یافتن یک عنصر خاص در آرایه یا لیست است.

* روش کار:

  1. پیمایش آرایه از ابتدا تا انتها
  2. مقایسه هر عنصر با مقدار مورد نظر
  3. اگر عنصر یافت شد → موقعیت (اندیس) آن برگردانده می‌شود
  4. اگر هیچ عنصری پیدا نشد → یک مقدار نامعتبر (مثلاً -1) باز می‌گردد

---

### 2️⃣ مثال

آرایه: `[4, 2, 7, 1, 3]`
مقدار جستجو: `7`

* پیمایش:

  * بررسی 4 → نه
  * بررسی 2 → نه
  * بررسی 7 → بله → اندیس 2 برگردانده می‌شود

* اگر مقدار جستجو: `5` بود → هیچ عنصری یافت نمی‌شود → `-1` برگردانده می‌شود

---

### 3️⃣ ویژگی‌ها و نکات آموزشی

* **نام انگلیسی:** Linear Search
* **پیچیدگی زمانی:**

  * بدترین حالت: (O(n))
  * بهترین حالت: (O(1)) (اگر عنصر اولین عنصر باشد)
* **مزایا:** ساده و بدون نیاز به مرتب‌سازی
* **معایب:** برای آرایه‌های بزرگ کند است

---

### 🧪 مثال اجرا

```bash
./04_linear_search 5  7  4 2 7 1 3
```
5 = تعداد آرایه
7 = مقدار جستجو

خروجی:

```
2
```

شماره اندیس از 0