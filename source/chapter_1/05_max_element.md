## Finding Maximum Element

### تعریف

در این درس هدف پیدا کردن **بزرگ‌ترین مقدار** موجود در یک آرایه از اعداد صحیح است.

* ابتدا اولین عنصر آرایه به عنوان **بزرگ‌ترین مقدار موقت** در نظر گرفته می‌شود.
* سپس آرایه از عنصر دوم به بعد پیمایش می‌شود.
* اگر عنصری بزرگ‌تر از مقدار فعلی باشد، مقدار بزرگ‌ترین به‌روزرسانی می‌شود.
* در پایان، بزرگ‌ترین مقدار موجود در آرایه چاپ می‌شود.


### نکات آموزشی

* از ساده‌ترین کاربردهای **پیمایش آرایه** است.
* پایه‌ای برای مسائل مهم‌تر مانند:

  * پیدا کردن کمینه و بیشینه
  * الگوریتم‌های آماری
  * پیش‌پردازش داده‌ها
* پیچیدگی زمانی الگوریتم:
  **O(n)** (آرایه فقط یک‌بار پیمایش می‌شود)

---

### 🧪 مثال اجرا

```bash
./05_max_element 4  8 7 1 3
```

4 = تعداد آرایه

خروجی:

```
8
```