## Queue Display Operation

### تعریف

**نمایش صف (Display Queue)** عملیاتی است که در آن **تمام عناصر موجود در صف**
از **Front تا Rear** بدون تغییر در ساختار صف نمایش داده می‌شوند.

> این عملیات فقط برای **مشاهده وضعیت فعلی صف** استفاده می‌شود و
> **هیچ عنصری حذف یا اضافه نمی‌شود**.

---

### موقعیت نمایش در صف

```
Front --> [ 15 | 25 | 35 | 45 ] <-- Rear
```

در نمایش صف:

* از اندیس **Front** شروع می‌کنیم
* تا اندیس **Rear** ادامه می‌دهیم
* ترتیب کاملاً **FIFO** حفظ می‌شود

---

### الگوریتم نمایش صف

```
اگر صف خالی بود
    پیام "Queue is empty" را چاپ کن
در غیر این صورت
    برای i از Front تا Rear
        عنصر Queue[i] را چاپ کن
```

---

### مثال مرحله‌ای

فرض کنید صف به صورت زیر باشد:

```
Front = 1
Rear  = 4

Index: 0  1  2  3  4
Data : - 10 20 30 40
```

نمایش صف خروجی زیر را می‌دهد:

```
10 20 30 40
```

---

### ایده کلی

* نمایش صف یک **عملیات فقط خواندنی (Read-only)** است.
* هیچ تغییری در مقدار `front` یا `rear` ایجاد نمی‌شود.
* در پیاده‌سازی آرایه‌ای:

  * نمایش همیشه بین `front` و `rear` انجام می‌شود.
* این عملیات برای **دیباگ** و **بررسی وضعیت صف** بسیار مهم است.

---

### نکات آموزشی

* نمایش صف ≠ حذف عناصر
* ترتیب نمایش دقیقاً ترتیب ورود به صف است.
* پیچیدگی زمانی:
  **O(n)**
* اگر صف خالی باشد، چیزی برای نمایش وجود ندارد.

---

### 🧪 مثال اجرا

```bash
./03_queue_display 5 10 20 30 40
```

خروجی:

```
Queue elements:
10 20 30 40
```

### [Back](/readme.md)

