## Loop (Iteration)

### تعریف

**Loop** یا **حلقه** ساختاری در برنامه‌نویسی است که اجازه می‌دهد
**یک یا چند دستور چندین بار تکرار شوند**، بدون اینکه نیاز باشد آن‌ها را دوباره بنویسیم.

حلقه‌ها زمانی استفاده می‌شوند که:

* تعداد تکرارها مشخص باشد
* یا تا زمانی که یک شرط برقرار است، عملیات ادامه پیدا کند

در C++ سه نوع حلقه اصلی وجود دارد:

* `for`
* `while`
* `do-while`

---

### انواع حلقه‌ها (خلاصه)

#### 1️⃣ حلقه for

مناسب زمانی که **تعداد تکرار مشخص** است.

#### 2️⃣ حلقه while

تا زمانی که شرط برقرار باشد اجرا می‌شود.

#### 3️⃣ حلقه do-while

حداقل **یک بار اجرا می‌شود**، حتی اگر شرط برقرار نباشد.

---

### ایده کلی پیاده‌سازی

در این درس:

* عدد `n` از ورودی دریافت می‌شود
* با استفاده از **حلقه for**
* اعداد از `1` تا `n` چاپ می‌شوند

این مثال ساده‌ترین و پایه‌ای‌ترین کاربرد حلقه را نشان می‌دهد.

---

### نکات آموزشی

* حلقه‌ها پایه‌ی:

  * پردازش آرایه‌ها
  * الگوریتم‌های مرتب‌سازی
  * ساختمان داده‌ها
    هستند.
* انتخاب نوع حلقه به **منطق مسئله** بستگی دارد.
* استفاده نادرست می‌تواند باعث:

  * حلقه بی‌نهایت (Infinite Loop)
  * مصرف زیاد منابع
    شود.

---

### 🧪 مثال اجرا

```bash
./05_loops 5
```

خروجی:

```
1 2 3 4 5
```

### [Back](/readme.md)
