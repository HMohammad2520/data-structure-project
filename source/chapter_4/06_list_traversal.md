## Linked List Traversal

### تعریف

**Linked List Traversal** به معنی **حرکت کردن روی گره‌های لیست پیوندی از ابتدا تا انتها** است.

در این فرآیند:

* از **گره اول (Head)** شروع می‌کنیم
* در هر مرحله:

  * داده گره فعلی خوانده می‌شود
  * به گره بعدی (`next`) می‌رویم
* این کار تا رسیدن به `NULL` ادامه دارد

---

### ایده کلی

در این درس:

* تعداد عناصر `n` از ورودی گرفته می‌شود
* `n` عدد به‌عنوان داده گره‌ها دریافت می‌شود
* یک **لیست پیوندی یک‌طرفه** ساخته می‌شود
* لیست از ابتدا تا انتها **پیمایش (Traversal)** شده
* داده‌ها به ترتیب چاپ می‌شوند

---

### نکات آموزشی

* پیمایش پایه‌ی تمام عملیات لیست پیوندی است:

  * جستجو
  * حذف
  * ویرایش
* پیچیدگی زمانی پیمایش:
  **O(n)**
* در لیست پیوندی:

  * برای رسیدن به عنصر آخر باید کل مسیر طی شود
* این مثال تفاوت اصلی لیست پیوندی و آرایه را به‌خوبی نشان می‌دهد.

---

### 🧪 مثال اجرا

```bash
./linked_list_traversal 5 10 20 30 40 50
```

خروجی:

```
10 20 30 40 50
```

### [Back](/readme.md)
