## Linked List

### تعریف

**Linked List (لیست پیوندی)** یک ساختار داده‌ی **غیرپیوسته** است که از مجموعه‌ای از **گره‌ها (Node)** تشکیل شده است.

هر گره شامل دو بخش است:

* **Data** → داده
* **Link / Next** → آدرس گره بعدی

برخلاف آرایه:

* عناصر پشت سر هم در حافظه نیستند
* اندازه لیست **پویا (Dynamic)** است

---

### ساختار یک گره (Node)

```
+-------+--------+
| Data  |  Next  |
+-------+--------+
```

نمایش مفهومی لیست:

```
Head
 ↓
[10|•] → [20|•] → [30|•] → NULL
```

---

### انواع لیست پیوندی

* **Singly Linked List** (تک‌پیوندی)
* **Doubly Linked List** (دو‌پیوندی)
* **Circular Linked List** (حلقوی)

در این درس:
👉 فقط **لیست پیوندی تک‌پیوندی** بررسی می‌شود.

---

### عملیات اصلی در Linked List

* ایجاد لیست
* درج عنصر (Insertion)
* حذف عنصر (Deletion)
* پیمایش و نمایش (Traversal)

در این مثال تمرکز روی:
👉 **ایجاد و نمایش لیست پیوندی**

---

### الگوریتم نمایش لیست پیوندی

```
اگر Head == NULL
    لیست خالی است
در غیر این صورت
    یک اشاره‌گر temp = Head
    تا وقتی temp != NULL
        داده temp را چاپ کن
        temp = temp->next
```

---

### مثال مرحله‌ای

فرض کنید عناصر زیر وارد لیست شوند:

```
10 , 20 , 30
```

ساختار لیست:

```
Head → [10] → [20] → [30] → NULL
```

خروجی نمایش:

```
10 20 30
```

---

### ایده کلی

* هر عنصر به عنصر بعدی **اشاره (Pointer)** دارد.
* دسترسی تصادفی مانند آرایه وجود ندارد.
* برای رسیدن به یک عنصر باید لیست **پیمایش (Traverse)** شود.
* درج و حذف نسبت به آرایه **کارآمدتر** است.

---

### نکات آموزشی

* استفاده زیاد از **Pointer**
* عدم نیاز به اندازه اولیه
* پیچیدگی زمانی نمایش:
  **O(n)**
* مناسب برای پیاده‌سازی:

  * Stack
  * Queue
  * Graph

---

### 🧪 مثال اجرا

```bash
./04_linked_list 3 10 20 30
```

خروجی:

```
10 20 30
```

### [Back](/readme.md)
