## 📘 تعریف کلی — Queue (صف)

**صف (Queue)** یک ساختمان داده‌ی **خطی (Linear Data Structure)** است که از قانون **FIFO** پیروی می‌کند:

> **FIFO – First In, First Out**
> اولین عنصری که وارد صف می‌شود، اولین عنصری است که خارج می‌شود.

### عملیات اصلی صف

| عملیات       | توضیح                                     |
| ------------ | ----------------------------------------- |
| Enqueue      | افزودن عنصر به انتهای صف                  |
| Dequeue      | حذف عنصر از ابتدای صف                     |
| Front / Peek | مشاهده عنصر اول بدون حذف                  |
| IsEmpty      | بررسی خالی بودن صف                        |
| IsFull       | بررسی پر بودن صف (در پیاده‌سازی آرایه‌ای) |

---

## 📘 مفاهیم LIFO و FIFO

| مفهوم    | توضیح                                           |
| -------- | ----------------------------------------------- |
| **FIFO** | First In, First Out → صف (Queue) مثال بارز است  |
| **LIFO** | Last In, First Out → پشته (Stack) مثال بارز است |

### مثال مفهومی

**Queue (FIFO)**:

```
Enqueue: 10, 20, 30
Dequeue → 10
Dequeue → 20
```

**Stack (LIFO)**:

```
Push: 10, 20, 30
Pop → 30
Pop → 20
```

---

### 🧪 مثال اجرا

```bash
./01_queue_fifo_lifo 10 20 30 dequeue 40
```

خروجی:

```
ENQUEUE 10
ENQUEUE 20
ENQUEUE 30
DEQUEUE 10
ENQUEUE 40
FRONT 20
```

### [Back](/readme.md)