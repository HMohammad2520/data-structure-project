## Linear Search

### تعریف

**Linear Search** یعنی **جستجو کردن عنصر مورد نظر در آرایه به صورت خطی**، از ابتدا تا انتها.

در این درس:

* طول آرایه (`n`) و عناصر آن از **ورودی برنامه** گرفته می‌شوند
* یک مقدار **کلید (`key`)** برای جستجو دریافت می‌شود
* آرایه از ابتدا تا انتها پیمایش می‌شود تا **موقعیت کلید** پیدا شود

---

### ایده کلی

در این درس:

* آرایه با اندازه پویا (`new`) ساخته می‌شود
* حلقه‌ای از ابتدا تا انتها بررسی می‌کند که آیا عنصر فعلی برابر کلید است یا نه
* اگر پیدا شد، **اندیس عنصر** چاپ می‌شود
* اگر پیدا نشد، مقدار `-1` چاپ می‌شود
* در پایان حافظه آرایه آزاد می‌شود (`delete[]`)

---

### نکات آموزشی

* ساده‌ترین روش جستجو در آرایه‌ها
* پیچیدگی زمانی: **O(n)**
* مناسب برای آرایه‌های **کوچک و بدون مرتب‌سازی**
* تفاوت با جستجوی دودویی:

  * نیاز به مرتب بودن آرایه ندارد

---

### 🧪 مثال اجرا

```bash
./04_linear_search 5 10 20 30 40 50 30
```

خروجی:

```
2
```

*(عنصر `30` در اندیس ۲ قرار دارد)*

### [Back](/readme.md)