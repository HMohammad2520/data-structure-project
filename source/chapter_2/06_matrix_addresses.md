## 2D Matrix Addresses

### تعریف

**2D Matrix Addresses** یعنی **دسترسی به آدرس حافظه عناصر یک ماتریس دوبعدی پویا**.

در این درس:

* تعداد سطر (`r`) و ستون (`c`) ماتریس از **ورودی برنامه** گرفته می‌شود
* عناصر ماتریس به صورت پویا (`new`) ذخیره می‌شوند
* با استفاده از حلقه‌های تودرتو **آدرس تمام عناصر** چاپ می‌شود

---

### ایده کلی

در این درس:

* ماتریس دوبعدی با آرایه‌ای از اشاره‌گرها (`int**`) ساخته می‌شود
* برای هر سطر، آرایه‌ای از `c` عنصر تخصیص داده می‌شود
* با دو حلقه تو در تو:

  * حلقه خارجی روی سطرها
  * حلقه داخلی روی ستون‌ها
  * آدرس هر عنصر با `&m[i][j]` چاپ می‌شود
* در پایان حافظه تمام سطرها و خود ماتریس آزاد می‌شود

---

### نکات آموزشی

* این درس تفاوت **آرایه دو بعدی ثابت و پویا** را نشان می‌دهد
* استفاده از **Pointer to Pointer** برای ساخت ماتریس پویا ضروری است
* پیچیدگی زمانی چاپ آدرس‌ها:
  **O(r × c)**
* این درس پایه‌ی **آموزش Pointer و Memory Layout** در C/C++ است

---

### 🧪 مثال اجرا

```bash
./06_matrix_addresses 2 3 1 2 3 4 5 6
```

خروجی نمونه (آدرس‌ها ممکن است متفاوت باشند):

```
0x600003d000 0x600003d004 0x600003d008
0x600003d00c 0x600003d010 0x600003d014
```

### [Back](/readme.md)
